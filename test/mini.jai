
#import "Basic"

x1 : float = .000257;
//s := "Hello there";

a : bool;

firstPtr: *u32;
secondPtr : *u32;

main :: ()
{
    te : u32;
    // x1 := 1;
    // x1 = add(3, 0);

    firstPtr = *te;

    // a = x1 < 4;
    // printf("X is %llu\n", x1);

//    x = #run (3 + 2);
}

add := (x: u64, y: u64) -> u64 
{
    return x + y;
}

// #run main()


