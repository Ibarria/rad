#import "Basic"

sum :: (a: int, b: float) -> int{
	return a;
}

main::()
{
 	A := 42;
	bb : bool = true;
	ba := false;
	AA := 32;
	c :: 3;
	bb = ba;

	print("%d times %d is %d\n", A, AA, A*AA);
	print("Also, %d / %d is %d\n", A, c, A/c);
}